# auto-generated by grc.converter

id: rstt_decoder
label: Decoder
category: '[RSTT]'

parameters:
-   id: sync_nbits
    label: Synchronize N bits
    dtype: int
    default: 20*10
-   id: sync_nbytes
    label: Synchronize N bytes
    dtype: int
    default: '32'
-   id: drop_invalid
    label: Drop invalid frames
    dtype: bool
    default: 'True'
-   id: guess_level
    label: Guess corrections
    dtype: int
    default: '2'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: short
    vlen: 240

templates:
    imports: import rstt
    make: rstt.decoder(${sync_nbits}, ${sync_nbytes}, ${drop_invalid})

documentation: |-
    Guess correction set level of 'guessing' for error correction based on
        byte value distribution probability.
        0 - disabled,
        1 - correct only well known bytes,
        4/6/8 - good values for value guessing (with error probability N/256)
        256 - try guess all bytes (nonsens)

file_format: 1
