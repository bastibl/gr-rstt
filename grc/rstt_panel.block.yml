# auto-generated by grc.converter

id: rstt_panel
label: RSTT Panel
category: '[RSTT]'

parameters:
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part

inputs:
-   domain: stream
    dtype: short
    vlen: 240

templates:
    imports: import rstt
    make: |
        <%
            win = 'self._%s_win'%id
        %>\
        rstt.rsttPanel()
        ${win} = self.${id}
        ${gui_hint() % win}

file_format: 1
