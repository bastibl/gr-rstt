# auto-generated by grc.converter

id: rstt_clip
label: clip
category: '[RSTT]'

parameters:
-   id: vlen
    label: Vector length
    dtype: int
    default: '1024'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: in_bw
    label: BW of input vector
    dtype: float
    default: '1.'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: out_bw
    label: BW of output vector
    dtype: float
    default: '0.8'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen - 2 * round(vlen*(in_bw - out_bw) / in_bw / 2) }

templates:
    imports: import rstt
    make: rstt.clip(${vlen}, ${in_bw}, ${out_bw})

documentation: |-
    Remove symetricaly outer points from input vector.

file_format: 1
